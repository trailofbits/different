[model]
# Default model for runs. Override per-run with `--model`.
name = "gpt-5.2"
provider = "openai"
reasoning_effort = "xhigh"
temperature = 0.0

[extract]
since_days = 30
max_commits = 50
max_patch_lines = 400
include_github = true
max_issues = 50
max_prs = 50

[reports]
html = true

